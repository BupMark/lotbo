name: Scrape Tikerama

on:
  schedule:
    - cron: '20 4 * * *'
  workflow_dispatch:
jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Appel route scrape-tikerama
        run: |
          curl -f -s -o /dev/null -w "%{http_code}" --max-time 90 \
            -H "x-internal-secret: ${{ secrets.INTERNAL_API_SECRET }}" \
            "https://app.lotbo.app/api/cron/scrape-tikerama"
