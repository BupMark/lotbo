name: Scrape La Londe-les-Maures
on:
  schedule:
    - cron: '45 3 * * *'
  workflow_dispatch:
jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Appel route scrape-lalondelesmaures
        run: |
          curl -f -s -o /dev/null -w "%{http_code}" --max-time 90 \
            -H "x-internal-secret: ${{ secrets.INTERNAL_API_SECRET }}" \
            "https://app.lotbo.app/api/cron/scrape-lalondelesmaures"
